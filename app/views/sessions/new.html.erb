<h1>Session#new</h1>
<%= form_for(:sessions, url: login_path) do |f| %>
<%# <%= render 'shared/errors', object: f.object %> 
  <%= f.label :email %>:
  <%= f.email_field :email %>
  <br>

  <%= f.submit 'Login' %>
<% end %>